[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-baselines-hle-tools-v0"
version = "0.1.0"
requires-python = ">=3.11,<3.14"
dependencies = [
    "astabench==0.5.3",
    "inspect_ai==0.3.203",
    "datasets==3.2.0",
    "httpx>=0.27,<1",
    "pillow>=10,<12",
    "pyyaml>=6,<7",
    "pytest>=8,<9",
]
